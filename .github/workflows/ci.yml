name: Node.js CI Pipeline

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build-and-test:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4  # Diperbarui ke v4

    - name: Setup Node.js
      uses: actions/setup-node@v4  # Diperbarui ke v4
      with:
        node-version: '22.x'       # Diperbarui ke versi LTS yang lebih baru

    - name: Install dependencies
      run: npm install

    - name: Run tests
      run: npm test